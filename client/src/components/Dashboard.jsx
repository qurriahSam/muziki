import React from "react";

function Dashboard({ user, songUpload }) {
  return (
    <div>
      <audio controls>
        <source src="#" type="audio/ogg" />
        Your browser does not support the audio element.
      </audio>
    </div>
  );
}

export default Dashboard;
